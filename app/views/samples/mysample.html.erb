<div class="container">

    <% @samples.each do |sample| %>
<% if sample.user == current_user %>

            <div class="row sample"> 
              <div class="col-md-12">
             <strong> Owner: <%= sample.user.name %></strong>
              <p><strong>Email:</strong> <%= sample.user.email %> </p>
             </div>
           </div>

    
              <div class="row"> 
               
               <div class="col-md-6">
                  <p><strong>Season</strong>
                  <%= sample.season %></p>
                   <p><strong>Style</strong>
                  <%= sample.style %></p>
                    <p> <strong>Line</strong>
                  <%= sample.line %></p>
                </div>

                <div class="col-md-6">
                  <p><strong>Color</strong>
                  <%= sample.color%></p>
                  <p><strong>Borrowed By:</strong>
                  <%= sample.to_who %></p>
                </div>

              </div>
              <div class="row"> 

                <div class="col-md-12 bottom">
                 <%= link_to 'View Sample', sample, class: "btn btn-md btn-default"if policy(sample).show? %>
                  <%= link_to 'Check In | Out', edit_sample_path(sample), class: "btn btn-md btn-default"  %>
                </div>
              </div>
              <% end %>
        <% end %> 

</div>
